# 阿里云部署指南

本指南将帮助您将实验室网站部署到阿里云服务器上。

## 📋 目录

1. [准备工作](#准备工作)
2. [购买阿里云服务器](#购买阿里云服务器)
3. [连接服务器](#连接服务器)
4. [环境配置](#环境配置)
5. [上传网站文件](#上传网站文件)
6. [配置Web服务器](#配置web服务器)
7. [配置域名](#配置域名)
8. [配置SSL证书（HTTPS）](#配置ssl证书https)
9. [常见问题](#常见问题)

---

## 准备工作

### 需要准备的内容：

1. **阿里云账号**（如果没有，请先注册）
2. **网站文件**（当前项目文件夹）
3. **域名**（可选，但推荐）
4. **SSH客户端**（Windows推荐使用 PuTTY 或 Xshell，Mac/Linux 可直接使用终端）

---

## 购买阿里云服务器

### 1. 登录阿里云控制台

访问 [阿里云官网](https://www.aliyun.com/)，登录您的账号。

### 2. 选择云服务器ECS

- 进入 **产品与服务** → **云服务器ECS**
- 点击 **立即购买** 或 **创建实例**

### 3. 配置服务器参数

#### 基础配置：
- **付费模式**：包年包月（推荐）或按量付费
- **地域**：选择离您用户最近的地域（如：华东1-杭州）
- **实例规格**：对于静态网站，选择 **1核2GB** 即可（约 ¥30-50/月）
- **镜像**：选择 **Ubuntu 22.04 LTS** 或 **CentOS 7.x**（推荐Ubuntu）

#### 网络配置：
- **网络类型**：专有网络VPC
- **公网IP**：分配公网IPv4地址（必须）
- **带宽**：1-5Mbps（根据访问量选择）

#### 系统配置：
- **登录凭证**：选择 **自定义密码** 或 **密钥对**
- 设置 **root密码**（请妥善保管）

#### 分组设置：
- 安全组：选择 **开放80端口（HTTP）** 和 **443端口（HTTPS）**

### 4. 完成购买

确认配置后，点击 **确认订单** 并完成支付。

---

## 连接服务器

### Windows系统

#### 方法1：使用PuTTY

1. 下载并安装 [PuTTY](https://www.putty.org/)
2. 打开PuTTY，输入：
   - **Host Name**: `您的公网IP地址`
   - **Port**: `22`
   - **Connection type**: `SSH`
3. 点击 **Open**
4. 输入用户名：`root`
5. 输入密码（输入时不会显示，直接输入后按回车）

#### 方法2：使用Xshell

1. 下载 [Xshell](https://www.netsarang.com/zh/xshell/)
2. 新建会话，输入服务器IP和端口22
3. 使用root账号和密码登录

### Mac/Linux系统

直接在终端执行：

```bash
ssh root@您的公网IP地址
```

然后输入密码即可。

---

## 环境配置

### 1. 更新系统

```bash
# Ubuntu系统
apt update && apt upgrade -y

# CentOS系统
yum update -y
```

### 2. 安装Nginx

```bash
# Ubuntu系统
apt install nginx -y

# CentOS系统
yum install nginx -y
```

### 3. 启动Nginx服务

```bash
# 启动Nginx
systemctl start nginx

# 设置开机自启
systemctl enable nginx

# 检查状态
systemctl status nginx
```

### 4. 配置防火墙

```bash
# Ubuntu系统（使用ufw）
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 22/tcp
ufw enable

# CentOS系统（使用firewalld）
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload
```

### 5. 测试访问

在浏览器中访问 `http://您的公网IP`，如果看到Nginx欢迎页面，说明配置成功。

---

## 上传网站文件

### 方法1：使用FTP工具（推荐）

#### 1. 安装FTP服务器（VSFTPD）

```bash
# Ubuntu系统
apt install vsftpd -y

# CentOS系统
yum install vsftpd -y

# 启动服务
systemctl start vsftpd
systemctl enable vsftpd
```

#### 2. 配置VSFTPD

```bash
# 编辑配置文件
nano /etc/vsftpd.conf
```

修改以下配置：

```
anonymous_enable=NO
local_enable=YES
write_enable=YES
local_umask=022
chroot_local_user=YES
```

保存后重启服务：

```bash
systemctl restart vsftpd
```

#### 3. 使用FileZilla上传文件

1. 下载 [FileZilla](https://filezilla-project.org/)
2. 新建站点：
   - **协议**: SFTP
   - **主机**: 您的公网IP
   - **端口**: 22
   - **用户名**: root
   - **密码**: 您的root密码
3. 连接后，将本地 `实验室网页` 文件夹中的所有文件上传到服务器的 `/var/www/html/` 目录

### 方法2：使用SCP命令

在本地电脑的终端执行：

```bash
# Windows (使用PowerShell或Git Bash)
scp -r 实验室网页/* root@您的公网IP:/var/www/html/

# Mac/Linux
scp -r 实验室网页/* root@您的公网IP:/var/www/html/
```

### 方法3：使用Git（如果项目在Git仓库）

```bash
# 在服务器上安装Git
apt install git -y  # Ubuntu
# 或
yum install git -y  # CentOS

# 克隆项目
cd /var/www/html
git clone 您的Git仓库地址
```

---

## 配置Web服务器

### 1. 配置Nginx

```bash
# 编辑Nginx配置文件
nano /etc/nginx/sites-available/default
```

### 2. 修改配置文件

将以下内容替换到配置文件中：

```nginx
server {
    listen 80;
    server_name 您的域名或IP地址;
    root /var/www/html/实验室网页;
    index index.html;

    # 字符编码
    charset utf-8;

    # 日志文件
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # 主页面配置
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}
```

**注意**：如果您的文件直接放在 `/var/www/html/` 下，将 `root /var/www/html/实验室网页;` 改为 `root /var/www/html;`

### 3. 测试配置

```bash
# 测试Nginx配置是否正确
nginx -t
```

如果显示 `syntax is ok` 和 `test is successful`，说明配置正确。

### 4. 重启Nginx

```bash
systemctl restart nginx
```

### 5. 设置文件权限

```bash
# 设置网站目录权限
chown -R www-data:www-data /var/www/html
chmod -R 755 /var/www/html
```

---

## 配置域名

### 1. 域名解析

1. 登录阿里云控制台
2. 进入 **域名** → **域名解析**
3. 找到您的域名，点击 **解析设置**
4. 添加记录：
   - **记录类型**: A
   - **主机记录**: @ 或 www
   - **记录值**: 您的服务器公网IP
   - **TTL**: 10分钟（默认）

### 2. 修改Nginx配置

如果有域名，修改Nginx配置中的 `server_name`：

```nginx
server_name www.您的域名.com 您的域名.com;
```

然后重启Nginx：

```bash
systemctl restart nginx
```

---

## 配置SSL证书（HTTPS）

### 方法1：使用Let's Encrypt免费证书（推荐）

#### 1. 安装Certbot

```bash
# Ubuntu系统
apt install certbot python3-certbot-nginx -y

# CentOS系统
yum install certbot python3-certbot-nginx -y
```

#### 2. 获取证书

```bash
certbot --nginx -d 您的域名.com -d www.您的域名.com
```

按照提示输入邮箱地址，选择是否重定向HTTP到HTTPS。

#### 3. 自动续期

Let's Encrypt证书有效期90天，设置自动续期：

```bash
# 测试续期
certbot renew --dry-run

# 添加定时任务（自动续期）
crontab -e
```

添加以下行：

```
0 0 1 * * certbot renew --quiet
```

### 方法2：使用阿里云SSL证书

1. 在阿里云控制台购买或申请免费SSL证书
2. 下载证书文件（Nginx格式）
3. 上传到服务器：`/etc/nginx/ssl/`
4. 修改Nginx配置：

```nginx
server {
    listen 443 ssl http2;
    server_name 您的域名.com;
    
    ssl_certificate /etc/nginx/ssl/证书文件名.pem;
    ssl_certificate_key /etc/nginx/ssl/证书密钥文件.key;
    
    # SSL优化配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    root /var/www/html/实验室网页;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# HTTP重定向到HTTPS
server {
    listen 80;
    server_name 您的域名.com;
    return 301 https://$server_name$request_uri;
}
```

重启Nginx：

```bash
systemctl restart nginx
```

---

## 常见问题

### 1. 无法访问网站

**检查清单：**
- ✅ 服务器是否运行：`systemctl status nginx`
- ✅ 防火墙是否开放80/443端口
- ✅ 安全组是否开放80/443端口
- ✅ Nginx配置是否正确：`nginx -t`
- ✅ 文件是否上传到正确目录

### 2. 403 Forbidden错误

```bash
# 检查文件权限
ls -la /var/www/html

# 修复权限
chown -R www-data:www-data /var/www/html
chmod -R 755 /var/www/html
```

### 3. 404 Not Found错误

- 检查 `index.html` 文件是否存在
- 检查Nginx配置中的 `root` 路径是否正确
- 检查文件是否在正确的目录下

### 4. 图片或资源加载失败

- 检查文件路径是否正确（相对路径 vs 绝对路径）
- 检查文件权限
- 检查Nginx的静态资源配置

### 5. 中文乱码问题

在Nginx配置中添加：

```nginx
charset utf-8;
```

### 6. 查看Nginx错误日志

```bash
tail -f /var/log/nginx/error.log
```

### 7. 重启Nginx服务

```bash
systemctl restart nginx
```

---

## 性能优化建议

### 1. 启用Gzip压缩

在Nginx配置中添加：

```nginx
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
```

### 2. 启用浏览器缓存

已在配置文件中添加静态资源缓存配置。

### 3. CDN加速（可选）

如果访问量大，可以考虑使用阿里云CDN加速静态资源。

---

## 备份建议

### 定期备份网站文件

```bash
# 创建备份脚本
nano /root/backup.sh
```

添加内容：

```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
tar -czf /root/backup_$DATE.tar.gz /var/www/html
# 保留最近7天的备份
find /root -name "backup_*.tar.gz" -mtime +7 -delete
```

设置执行权限：

```bash
chmod +x /root/backup.sh
```

添加到定时任务：

```bash
crontab -e
# 每天凌晨2点备份
0 2 * * * /root/backup.sh
```

---

## 安全建议

1. **定期更新系统**：`apt update && apt upgrade`
2. **修改SSH端口**（可选）：修改 `/etc/ssh/sshd_config`
3. **禁用root登录**（可选）：使用普通用户+sudo
4. **安装防火墙**：已配置ufw或firewalld
5. **定期备份**：设置自动备份脚本
6. **监控服务器**：使用阿里云云监控

---

## 总结

完成以上步骤后，您的网站应该已经成功部署到阿里云服务器上。可以通过以下方式访问：

- **HTTP**: `http://您的域名` 或 `http://您的公网IP`
- **HTTPS**: `https://您的域名`（如果配置了SSL证书）

如有问题，请参考常见问题部分或查看Nginx错误日志。

---

## 联系支持

- 阿里云技术支持：https://workorder.console.aliyun.com/
- Nginx官方文档：https://nginx.org/en/docs/

---

**祝您部署顺利！** 🚀

