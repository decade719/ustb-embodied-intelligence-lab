# 免费部署指南 - USTB具身智能实验室

## 🌐 网站名称建议

**推荐名称：**
- **USTB具身智能实验室** (USTB Embodied Intelligence Lab)
- **北京科技大学具身智能实验室官网**
- **北科大具身智能实验室**

---

## 🆓 免费部署方案

### 方案一：Vercel 部署（推荐 ⭐⭐⭐⭐⭐）

**优点：**
- ✅ 完全免费
- ✅ 自动HTTPS
- ✅ 全球CDN加速
- ✅ 自动部署（连接GitHub）
- ✅ 自定义域名支持
- ✅ 部署速度快

#### 部署步骤：

1. **准备GitHub仓库**
   ```bash
   # 在项目根目录执行
   cd 实验室网页
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **创建GitHub仓库**
   - 访问 https://github.com
   - 点击右上角 "+" → "New repository"
   - 仓库名：`ustb-embodied-intelligence-lab`（或您喜欢的名字）
   - 选择 Public（公开）
   - 点击 "Create repository"

3. **推送代码到GitHub**
   ```bash
   git remote add origin https://github.com/您的用户名/ustb-embodied-intelligence-lab.git
   git branch -M main
   git push -u origin main
   ```

4. **部署到Vercel**
   - 访问 https://vercel.com
   - 使用GitHub账号登录
   - 点击 "Add New Project"
   - 选择您的仓库
   - **重要配置**：
     - Framework Preset: `Other`
     - Root Directory: `实验室网页`（如果仓库在根目录，留空）
     - Build Command: 留空（静态网站不需要构建）
     - Output Directory: `.`（当前目录）
   - 点击 "Deploy"
   - 等待1-2分钟，部署完成！

5. **访问网站**
   - Vercel会自动分配一个域名，如：`your-project.vercel.app`
   - 也可以绑定自定义域名（免费）

---

### 方案二：Netlify 部署（推荐 ⭐⭐⭐⭐）

**优点：**
- ✅ 完全免费
- ✅ 自动HTTPS
- ✅ 全球CDN
- ✅ 拖拽部署（最简单）
- ✅ 表单处理功能

#### 部署步骤：

1. **准备网站文件**
   - 确保所有文件在 `实验室网页` 文件夹中

2. **部署方式A：拖拽部署（最简单）**
   - 访问 https://www.netlify.com
   - 注册/登录账号
   - 直接将 `实验室网页` 文件夹拖拽到Netlify页面
   - 等待上传完成，自动部署！

3. **部署方式B：GitHub连接**
   - 登录Netlify
   - 点击 "Add new site" → "Import an existing project"
   - 选择GitHub，授权后选择仓库
   - 配置：
     - Base directory: `实验室网页`（如果仓库在根目录，留空）
     - Build command: 留空
     - Publish directory: `.`
   - 点击 "Deploy site"

4. **访问网站**
   - Netlify会分配域名：`your-project.netlify.app`
   - 可绑定自定义域名

---

### 方案三：GitHub Pages 部署（推荐 ⭐⭐⭐）

**优点：**
- ✅ 完全免费
- ✅ 自动HTTPS
- ✅ 使用GitHub域名或自定义域名
- ✅ 与代码仓库集成

#### 部署步骤：

1. **创建GitHub仓库**（同上）

2. **推送代码到GitHub**（同上）

3. **启用GitHub Pages**
   - 进入仓库页面
   - 点击 "Settings" → "Pages"
   - Source: 选择 `main` 分支
   - Folder: 选择 `/实验室网页`（如果仓库在根目录，选择 `/ (root)`）
   - 点击 "Save"

4. **访问网站**
   - 地址：`https://您的用户名.github.io/仓库名/`
   - 如果仓库名是 `ustb-embodied-intelligence-lab`
   - 访问：`https://您的用户名.github.io/ustb-embodied-intelligence-lab/`

5. **配置自定义域名（可选）**
   - 在仓库根目录创建 `CNAME` 文件
   - 内容：`您的域名.com`
   - 在域名DNS中添加CNAME记录指向 `您的用户名.github.io`

---

### 方案四：Cloudflare Pages（推荐 ⭐⭐⭐⭐）

**优点：**
- ✅ 完全免费
- ✅ 全球最快的CDN
- ✅ 自动HTTPS
- ✅ 无限带宽

#### 部署步骤：

1. **准备GitHub仓库**（同上）

2. **部署到Cloudflare Pages**
   - 访问 https://pages.cloudflare.com
   - 使用GitHub账号登录
   - 点击 "Create a project"
   - 选择您的仓库
   - 配置：
     - Project name: `ustb-embodied-intelligence-lab`
     - Production branch: `main`
     - Build command: 留空
     - Build output directory: `实验室网页`（如果仓库在根目录，留空）
   - 点击 "Save and Deploy"

3. **访问网站**
   - 地址：`https://your-project.pages.dev`
   - 可绑定自定义域名

---

## 📝 部署前检查清单

### 1. 修改网站标题和元信息

在所有HTML文件的 `<head>` 中添加：

```html
<meta name="description" content="北京科技大学具身智能实验室 - 专注具身智能理论、机器人系统与跨学科应用">
<meta name="keywords" content="具身智能,机器人,USTB,北京科技大学,人工智能">
```

### 2. 检查所有链接

- ✅ 确保所有相对路径正确
- ✅ 图片路径使用相对路径（如：`pictures/xxx.jpg`）
- ✅ 内部链接使用相对路径（如：`cases.html`）

### 3. 测试本地运行

```bash
# 在实验室网页目录下
python -m http.server 8080
# 访问 http://localhost:8080
```

### 4. 优化文件大小

- 压缩图片（使用 TinyPNG 或类似工具）
- 删除未使用的文件

---

## 🔧 常见问题解决

### 问题1：页面404错误

**原因**：路径配置不正确

**解决**：
- Vercel/Netlify：在根目录创建 `vercel.json` 或 `netlify.toml`
- GitHub Pages：检查仓库设置中的路径配置

### 问题2：图片不显示

**原因**：路径大小写敏感

**解决**：
- 确保图片路径与实际文件名大小写完全一致
- Linux服务器区分大小写

### 问题3：样式丢失

**原因**：CSS文件路径错误

**解决**：
- 检查 `styles.css` 路径
- 确保使用相对路径：`href="styles.css"`

---

## 📦 创建部署配置文件

### Vercel配置（vercel.json）

在 `实验室网页` 目录创建 `vercel.json`：

```json
{
  "version": 2,
  "builds": [
    {
      "src": "**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
```

### Netlify配置（netlify.toml）

在 `实验室网页` 目录创建 `netlify.toml`：

```toml
[build]
  publish = "."
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### GitHub Pages配置（.nojekyll）

在 `实验室网页` 目录创建 `.nojekyll` 文件（空文件即可）：

```
（空文件，用于禁用Jekyll）
```

---

## 🚀 快速部署命令（推荐Vercel）

```bash
# 1. 安装Vercel CLI
npm install -g vercel

# 2. 进入项目目录
cd 实验室网页

# 3. 部署
vercel

# 4. 生产环境部署
vercel --prod
```

---

## 📊 各平台对比

| 平台 | 免费额度 | 速度 | 易用性 | 自定义域名 | 推荐度 |
|------|---------|------|--------|-----------|--------|
| Vercel | 无限 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ |
| Netlify | 100GB/月 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ |
| GitHub Pages | 1GB存储 | ⭐⭐⭐ | ⭐⭐⭐ | ✅ | ⭐⭐⭐ |
| Cloudflare Pages | 无限 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ |

---

## 🎯 推荐方案

**最佳选择：Vercel**
- 部署最简单
- 速度最快
- 完全免费
- 支持自动部署

**备选方案：Netlify**
- 拖拽部署最简单
- 功能丰富

---

## 📞 需要帮助？

如果部署过程中遇到问题，可以：
1. 查看平台官方文档
2. 检查浏览器控制台错误
3. 查看部署日志

---

**祝您部署顺利！** 🎉

